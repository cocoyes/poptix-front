import Link from 'next/link'; import {ArrowRight} from 'lucide-react'; import {events,pools} from '@/mock/data'
export default function Home(){return <div className="content"><section className="home-hero"><h1>A better market<br/>for live tickets.</h1><p>Buy, draw, trade and redeem tickets for the world&apos;s best live experiences — all in one market.</p><div className="actions"><Link className="btn primary" href="/events">Explore events <ArrowRight size={16}/></Link><Link className="btn secondary" href="/pools">Ticket pools</Link></div></section><section className="product-preview"><Link href="/marketplace" className="preview-panel"><div className="preview-label"><span>Marketplace</span><span className="muted">Open <ArrowRight size={14}/></span></div><div className="preview-canvas"><div className="preview-event" style={{backgroundImage:`url(${events[0].cover})`}}/><div className="preview-row"><b>{events[0].name}</b><span>Floor $310</span></div><div className="preview-row"><span>VIP2 · Section A1</span><b>$420</b></div><div className="preview-row"><span>CAT1 · Section B4</span><b>$328</b></div></div></Link><Link href={`/pools/${pools[0].id}`} className="preview-panel"><div className="preview-label"><span>Live ticket pools</span><span className="muted">Open <ArrowRight size={14}/></span></div><div className="preview-canvas"><div className="preview-event" style={{backgroundImage:`url(${pools[0].cover})`}}/><div className="preview-row"><b>{pools[0].name}</b><span>${pools[0].drawPrice} / draw</span></div><div className="preview-row"><span>VIP1</span><b>0.5%</b></div><div className="preview-row"><span>Remaining</span><b>{pools[0].remainingTickets} / {pools[0].totalTickets}</b></div></div></Link></section><section className="section"><div className="section-head"><h2>Market activity</h2><span className="small muted">Last 24 hours</span></div><div className="metric-strip"><div className="metric"><div className="metric-label">Trading volume</div><div className="metric-value">$2.84M</div></div><div className="metric"><div className="metric-label">Tickets traded</div><div className="metric-value">12,482</div></div><div className="metric"><div className="metric-label">Live markets</div><div className="metric-value">328</div></div></div></section><section className="section"><div className="section-head"><h2>Trending now</h2><Link href="/events">Explore all →</Link></div><div className="activity-list">{events.slice(0,6).map((event,i)=><Link className="activity-row" href={`/events/${event.id}`} key={event.id}><div className="activity-image" style={{backgroundImage:`url(${event.cover})`}}/><div><div className="strong">{event.name}</div><div className="activity-meta">{event.city} · {event.venue}</div></div><div className="activity-price"><div>${220+i*34}</div><div className="activity-meta">from</div></div><div className="activity-action">View market →</div></Link>)}</div></section></div>}
