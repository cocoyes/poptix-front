'use client'
import {useAppStore} from '@/stores/app'; import {Bell,Globe,LockKeyhole,Wallet} from 'lucide-react'
export default function Profile(){const {authenticated,login,showToast}=useAppStore();return <div className="content"><div className="eyebrow">Account</div><h1 className="title">Profile</h1><p className="subtitle">Manage your identity, preferences and connected wallet.</p><section className="section grid grid-2"><div className="card card-body"><div className="row"><div className="avatar" style={{width:64,height:64,fontSize:18}}>AL</div><div style={{marginLeft:14}}><div className="strong" style={{fontSize:18}}>Alex Lee</div><div className="small muted">alex@example.com</div><span className="badge" style={{marginTop:7}}>Gold member</span></div></div><div className="row" style={{marginTop:25,padding:'14px 0',borderTop:'1px solid var(--border)',borderBottom:'1px solid var(--border)'}}><span className="small muted">Mock wallet</span><span className="small">0x8B7...93A1</span></div><button className="btn primary" style={{width:'100%',marginTop:18}} onClick={()=>authenticated?showToast('Wallet is already connected'):login()}>{authenticated?'Connected':'Connect account'}</button></div><div className="card card-body">{[[Wallet,'Linked accounts','Google · WalletConnect'],[LockKeyhole,'Security','Passkeys and sessions'],[Bell,'Notifications','Price alerts · Pool draws'],[Globe,'Language & currency','English · USD']].map(([Icon,title,desc])=><button className="row" key={title as string} onClick={()=>showToast(`${title as string} settings opened in demo mode`)} style={{background:'none',border:0,color:'var(--text)',padding:'15px 0',borderBottom:'1px solid var(--border)',textAlign:'left'}}><span style={{display:'flex',alignItems:'center',gap:12}}><span className="icon-btn"><Icon size={16}/></span><span><span className="strong" style={{display:'block'}}>{title as string}</span><span className="small muted">{desc as string}</span></span></span><span className="muted">→</span></button>)}</div></section><div className="notice" style={{marginTop:28}}>Privy, stablecoins and identity verification will be connected here in a future production release.</div></div>}
